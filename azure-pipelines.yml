trigger:
  branches:
    include:
      - '*'

pool:
  vmImage: ubuntu-latest

resources:
  repositories:
    - repository: templates
      type: github
      name: rweritz/CICD

jobs:
  - template: ci-pipeline.yml@templates  # Template reference
    parameters:
      restoreFeed: excribe
      publishFeed: excribe