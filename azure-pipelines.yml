trigger:
  branches:
    include:
      - '*'

pool: excribeAgentPool

resources:
  repositories:
    - repository: templates
      type: github
      name: rweritz/CICD
      endpoint: rweritz

steps:
  - template: pipeline-templates/dotnet-ci-template.yml@templates
    parameters:
      restoreFeed: excribe
      publishFeed: excribe